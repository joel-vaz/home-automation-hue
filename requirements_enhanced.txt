# Core functionality
phue==1.1
SpeechRecognition==3.10.0
pyaudio==0.2.13
python-dotenv==1.0.0

# Wake word detection (optional, used in standard mode)
pvporcupine==2.2.1

# Enhanced functionality
plyer==2.1.0
fuzzywuzzy==0.18.0
python-Levenshtein==0.20.9

# For macOS notifications (optional)
pyobjus==1.2.3  # Uncomment if on macOS and want native notifications

# For Windows text-to-speech (optional)
# pyttsx3==2.90  # Uncomment if on Windows and want voice feedback

pydub==0.25.1
numpy==1.26.0 